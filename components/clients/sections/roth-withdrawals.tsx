"use client";

// Section intentionally hidden.
// The withdrawal_type field (no_withdrawals / systematic / penalty_free) is not
// wired into any calculation engine. Standard practice for Roth conversion
// projections is to leave the Roth bucket untouched as the legacy asset, so
// this section is hidden until/unless a real withdrawal feature is built.
export function RothWithdrawalsSection() {
  return null;
}
